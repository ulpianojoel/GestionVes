<Page x:Class="Ves.UI.Pages.OperationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignWidth="900"
      d:DesignHeight="680">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Operaciones" Style="{StaticResource TitleText}" />
            <TextBlock Text="Administrá el flujo de trabajo y los vencimientos"
                       Foreground="{StaticResource MutedTextBrush}"
                       Margin="0,4,0,16" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,16,0">
                <StackPanel>
                    <TextBlock Text="Listado de operaciones"
                               Style="{StaticResource SectionHeaderText}" />
                    <ListBox x:Name="OperationsList"
                             Margin="0,12,0,0"
                             SelectionChanged="OnOperationSelected">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#102D5BFF"
                                        BorderThickness="1"
                                        CornerRadius="12"
                                        Padding="12"
                                        Margin="0,0,0,12">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Description}" Foreground="{StaticResource MutedTextBrush}" />
                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <Border Background="#142D5BFF" CornerRadius="12" Padding="8,4">
                                                <TextBlock Text="{Binding Status}" Foreground="{StaticResource PrimaryBrush}" />
                                            </Border>
                                            <TextBlock Text="{Binding DueLabel}" Margin="12,0,0,0" Foreground="{StaticResource MutedTextBrush}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="Detalle de la operación" Style="{StaticResource SectionHeaderText}" />
                    <TextBlock x:Name="OperationTitle" FontSize="18" FontWeight="SemiBold" Margin="0,6,0,0" />
                    <TextBlock x:Name="OperationDescription" TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0" />
                    <ComboBox x:Name="StatusCombo"
                              Margin="0,16,0,0"
                              SelectionChanged="OnStatusChanged">
                        <ComboBoxItem Content="Pendiente" />
                        <ComboBoxItem Content="En progreso" />
                        <ComboBoxItem Content="Bloqueado" />
                        <ComboBoxItem Content="Completado" />
                    </ComboBox>
                    <DatePicker x:Name="DueDatePicker" Margin="0,12,0,0" SelectedDateChanged="OnDueDateChanged" />
                    <TextBlock x:Name="OperationMessage" Margin="0,16,0,0" Foreground="{StaticResource MutedTextBrush}" />
                    <Button Content="Marcar como completada"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Width="200"
                            Margin="0,20,0,0"
                            Click="OnCompleteClicked" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
